---
status: accepted
date: 2026-06-23
deciders: [scbd-bl2-team]
context: system-wide
origin: standalone
---

# 0001. Record architecture decisions

We will record architecturally-significant decisions as Architecture Decision Records in
`docs/adr/`, following the format in the team's `docs-adrs` skill (ADR-FORMAT.md). Decisions are
numbered sequentially and immutable once accepted; a decision is changed by superseding it with a
new ADR, never by editing the old one. The records that follow (0002 onward) are drawn
retroactively from decisions already in force in the `scbd_field` module, captured so a future
reader understands why the module is shaped the way it is.
