<?php

namespace Drupal\scbd_field\Utility;

/**
 * (Deprecated) DescriptionTemplateTrait removed as unused.
 *
 * Left as an empty stub temporarily to avoid fatal errors if stale caches
 * reference it. Safe to delete after a cache rebuild (drush cr) confirms
 * no references remain.
 */
trait DescriptionTemplateTrait
{
}
